package com.stjerncraft.controlpanel.core.user;

/**
 * User Manager takes care of creating, verifying and storing users.
 * 
 * As long as a Client is logged in with a User, that User's settings & permissions are cached in memory.
 * TODO: Send Permissions of active users to all Agents? So local calls can still do permission checks.
 */
public class UserManager {

}
